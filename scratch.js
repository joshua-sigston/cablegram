const { StreamChat } = require("stream-chat");

// Just a type-checking script to see if client.channel allows no ID.
// We can just statically check stream-chat SDK if it's available in node_modules.
// We can use ts-node to compile and see what happens.
